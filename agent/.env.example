# Vertex AI 配置
GCP_PROJECT_ID=tsmccareerhack2026-tsid-grp1
GCP_LOCATION=us-central1
VERTEX_AI_MODEL=gemini-2.5-pro

# Service Account Key (可選，替代 gcloud auth application-default login)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# MongoDB
MONGODB_URL=mongodb://localhost:27017

# Agent Runtime (容器執行時由 Backend 注入)
PROJECT_ID=test-project
RUN_ID=test-run-001
ITERATION_INDEX=1
INIT_PROMPT=請分析程式碼庫結構
