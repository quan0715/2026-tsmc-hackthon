# ============================================
# Agent 容器環境變數範例
# ============================================
# 此檔案定義容器內 AI Server 和 Deep Agent 所需的環境變數
# Runtime 時由 Backend 透過 Docker -e 參數注入

# ============================================
# 必填項目
# ============================================

# Anthropic API（主要 LLM Provider）
# Runtime 時由 Backend 從 backend/.env 讀取並注入
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# PostgreSQL（會話持久化 - 必填！）
# ⚠️ Agent 需要 PostgreSQL 儲存對話狀態和 checkpoints
# Runtime 時由 Backend 注入
POSTGRES_URL=postgresql://langgraph:langgraph_secret@postgres:5432/langgraph

# ============================================
# 可選項目
# ============================================

# Anthropic 模型設定
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# ============================================
# 說明
# ============================================
# 1. ANTHROPIC_API_KEY 必填 - 用於呼叫 Anthropic Claude API
# 2. POSTGRES_URL 必填 - Agent 無法在沒有 PostgreSQL 的情況下運行
# 3. 這些環境變數在容器啟動時由 Backend 動態注入
# 4. 容器內 AI Server 監聽 0.0.0.0:8000，透過 Docker 網路通訊
# 5. Agent 執行參數（init_prompt 等）透過 HTTP API 傳遞

# ============================================
# 開發模式說明
# ============================================
# Agent 容器在開發模式下不需要此檔案
# 所有環境變數會在容器啟動時從 backend/.env 注入
